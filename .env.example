# .env.example
# Base URL for API calls
# All API calls go through Session Gateway (same origin)
# Session Gateway adds JWT and forwards to NGINX → Backend services
VITE_API_BASE_URL=/api

# Development URL: https://app.budgetanalyzer.localhost (NGINX → Session Gateway)
# Production URL: Load balancer domain (e.g., budgetanalyzer.com)
#
# DO NOT access Vite dev server directly (http://localhost:3000)
# Always use https://app.budgetanalyzer.localhost for authentication to work
#
# Setup: Run orchestration/nginx/scripts/dev/setup-local-https.sh to generate SSL certificates

# Session heartbeat (optional, defaults shown)
# VITE_HEARTBEAT_INTERVAL_MS=300000         # 5 minutes
# VITE_WARNING_BEFORE_EXPIRY_SECONDS=300    # 5 minutes before expiry
